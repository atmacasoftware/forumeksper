const joinedChannels = document.querySelectorAll('.joined-channels');


joinedChannels.forEach(btn => {
    btn.addEventListener('mouseover', function () {
        let getID = btn.getAttribute('data-room-id');

    })
});